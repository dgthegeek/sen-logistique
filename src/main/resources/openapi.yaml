openapi: 3.0.3
info:
  title: Plateforme Logistique Dakar API
  description: |
    API pour la gestion de livraisons pour petits vendeurs Instagram/Facebook Ã  Dakar.
    
    **FonctionnalitÃ©s principales :**
    - Gestion des livraisons (crÃ©ation, suivi, statuts)
    - Ramassages groupÃ©s par zones
    - Notifications WhatsApp automatiques
    - Gestion financiÃ¨re (Cash on Delivery)
    - Tracking public des colis
    
    **Authentification :** JWT Bearer Token
  version: 1.0.0-MVP
  contact:
    name: Plateforme Logistique Dakar
    email: contact@votreplateforme.sn

servers:
  - url: http://localhost:8080/api
    description: Serveur de dÃ©veloppement
  - url: https://api.votreplateforme.sn/api
    description: Serveur de production

tags:
  - name: Authentication
    description: Endpoints d'authentification et gestion des comptes
  - name: Vendeur
    description: Endpoints pour les vendeurs (crÃ©ation livraisons, finances, etc.)
  - name: Admin - Vendeurs
    description: Gestion et validation des vendeurs
  - name: Admin - Ramassages
    description: Gestion des ramassages groupÃ©s
  - name: Admin - Livraisons
    description: Gestion des livraisons Ã  effectuer
  - name: Admin - Finances
    description: Gestion financiÃ¨re et paiements vendeurs
  - name: Delivery
    description: Confirmation de livraison (via scan QR)
  - name: Tracking
    description: Suivi public des colis
  - name: Zones
    description: Gestion des zones et quartiers

security:
  - bearerAuth: []

paths:
  # ============================================
  # AUTHENTICATION
  # ============================================
  /auth/register:
    post:
      tags:
        - Authentication
      summary: Inscription d'un nouveau vendeur
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RegisterRequest'
      responses:
        '201':
          description: Inscription rÃ©ussie
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuthResponse'
        '400':
          description: DonnÃ©es invalides
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '409':
          description: Utilisateur dÃ©jÃ  existant
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /auth/login:
    post:
      tags:
        - Authentication
      summary: Connexion
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LoginRequest'
      responses:
        '200':
          description: Connexion rÃ©ussie
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuthResponse'
        '401':
          description: Identifiants invalides
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /auth/refresh:
    post:
      tags:
        - Authentication
      summary: RafraÃ®chir le token JWT
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - refreshToken
              properties:
                refreshToken:
                  type: string
      responses:
        '200':
          description: Token rafraÃ®chi
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuthResponse'
        '401':
          description: Token invalide
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /auth/forgot-password:
    post:
      tags:
        - Authentication
      summary: Demande de rÃ©initialisation de mot de passe
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - telephone
              properties:
                telephone:
                  type: string
                  example: "771234567"
      responses:
        '200':
          description: Message de rÃ©initialisation envoyÃ©
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                    example: "Code de rÃ©initialisation envoyÃ© par WhatsApp"
        '404':
          description: Utilisateur non trouvÃ©
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  # ============================================
  # VENDEUR
  # ============================================
  /vendeur/dashboard:
    get:
      tags:
        - Vendeur
      summary: Tableau de bord du vendeur
      description: Statistiques du jour (total colis, livrÃ©s, en cours)
      responses:
        '200':
          description: Dashboard rÃ©cupÃ©rÃ© avec succÃ¨s
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VendeurDashboard'
        '401':
          $ref: '#/components/responses/UnauthorizedError'

  /vendeur/livraisons:
    get:
      tags:
        - Vendeur
      summary: Liste des livraisons du vendeur
      parameters:
        - name: statut
          in: query
          description: Filtrer par statut
          schema:
            $ref: '#/components/schemas/StatutLivraison'
        - name: page
          in: query
          schema:
            type: integer
            default: 0
        - name: size
          in: query
          schema:
            type: integer
            default: 20
      responses:
        '200':
          description: Liste des livraisons
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PageLivraison'
        '401':
          $ref: '#/components/responses/UnauthorizedError'

    post:
      tags:
        - Vendeur
      summary: CrÃ©er une nouvelle livraison
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateLivraisonRequest'
      responses:
        '201':
          description: Livraison crÃ©Ã©e avec succÃ¨s
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LivraisonResponse'
        '400':
          description: DonnÃ©es invalides
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          $ref: '#/components/responses/UnauthorizedError'

  /vendeur/livraisons/{id}:
    get:
      tags:
        - Vendeur
      summary: DÃ©tails d'une livraison
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: DÃ©tails de la livraison
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LivraisonDetailResponse'
        '404':
          description: Livraison non trouvÃ©e
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          $ref: '#/components/responses/UnauthorizedError'

  /vendeur/finances:
    get:
      tags:
        - Vendeur
      summary: Ã‰tat financier du vendeur
      description: Solde en attente, historique des paiements, statistiques du mois
      responses:
        '200':
          description: Finances rÃ©cupÃ©rÃ©es
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VendeurFinances'
        '401':
          $ref: '#/components/responses/UnauthorizedError'

  /vendeur/demande-paiement:
    post:
      tags:
        - Vendeur
      summary: Demander un paiement
      description: Le vendeur demande Ã  rÃ©cupÃ©rer son solde en cash
      responses:
        '200':
          description: Demande de paiement crÃ©Ã©e
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                    example: "Demande de paiement envoyÃ©e Ã  l'admin"
                  montant:
                    type: number
                    format: decimal
                    example: 387500
        '400':
          description: Solde insuffisant ou demande dÃ©jÃ  en cours
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          $ref: '#/components/responses/UnauthorizedError'

  # ============================================
  # ADMIN - RAMASSAGES
  # ============================================
  /admin/ramassages:
    get:
      tags:
        - Admin - Ramassages
      summary: Liste de tous les ramassages
      parameters:
        - name: date
          in: query
          description: Date des ramassages (format YYYY-MM-DD)
          schema:
            type: string
            format: date
        - name: zone
          in: query
          description: Filtrer par zone
          schema:
            type: string
      responses:
        '200':
          description: Liste des ramassages
          content:
            application/json:
              schema:
                type: object
                properties:
                  zones:
                    type: array
                    items:
                      $ref: '#/components/schemas/RamassageZone'
                  totalColis:
                    type: integer
                    example: 42
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'

  /admin/ramassages/today:
    get:
      tags:
        - Admin - Ramassages
      summary: Ramassages du jour
      description: Liste des colis en attente de ramassage aujourd'hui, groupÃ©s par zone
      responses:
        '200':
          description: Ramassages du jour
          content:
            application/json:
              schema:
                type: object
                properties:
                  zones:
                    type: array
                    items:
                      $ref: '#/components/schemas/RamassageZone'
                  totalColis:
                    type: integer
                    example: 12
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'

  /admin/ramassages/notifier-zone/{zoneNom}:
    post:
      tags:
        - Admin - Ramassages
      summary: Notifier les vendeurs d'une zone
      description: Envoie un message WhatsApp groupÃ© aux vendeurs pour prÃ©parer leurs colis
      parameters:
        - name: zoneNom
          in: path
          required: true
          schema:
            type: string
            example: "SacrÃ©-Coeur"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                heureRamassage:
                  type: string
                  example: "15h"
      responses:
        '200':
          description: Notifications envoyÃ©es
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                    example: "3 vendeurs notifiÃ©s"
                  vendeursNotifies:
                    type: integer
                    example: 3
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'

  /admin/ramassages/marquer-ramasse:
    post:
      tags:
        - Admin - Ramassages
      summary: Marquer des colis comme ramassÃ©s
      description: Change le statut des colis en "RAMASSE" et envoie les notifications
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - livraisonIds
              properties:
                livraisonIds:
                  type: array
                  items:
                    type: integer
                    format: int64
                  example: [1, 2, 3, 4, 5]
      responses:
        '200':
          description: Colis marquÃ©s comme ramassÃ©s
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                    example: "5 colis marquÃ©s comme ramassÃ©s"
                  colisRamasses:
                    type: integer
                    example: 5
        '400':
          description: Erreur dans les IDs fournis
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'

  /admin/ramassages/imprimer-qr:
    post:
      tags:
        - Admin - Ramassages
      summary: GÃ©nÃ©rer un PDF avec les QR codes
      description: GÃ©nÃ¨re un PDF avec tous les QR codes des colis ramassÃ©s
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - livraisonIds
              properties:
                livraisonIds:
                  type: array
                  items:
                    type: integer
                    format: int64
                  example: [1, 2, 3]
      responses:
        '200':
          description: PDF gÃ©nÃ©rÃ©
          content:
            application/pdf:
              schema:
                type: string
                format: binary
        '400':
          description: Erreur dans les IDs fournis
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'

  # ============================================
  # ADMIN - LIVRAISONS
  # ============================================
  /admin/livraisons:
    get:
      tags:
        - Admin - Livraisons
      summary: Liste de toutes les livraisons
      parameters:
        - name: statut
          in: query
          schema:
            $ref: '#/components/schemas/StatutLivraison'
        - name: date
          in: query
          schema:
            type: string
            format: date
        - name: page
          in: query
          schema:
            type: integer
            default: 0
        - name: size
          in: query
          schema:
            type: integer
            default: 50
      responses:
        '200':
          description: Liste des livraisons
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PageLivraison'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'

  /admin/livraisons/a-livrer:
    get:
      tags:
        - Admin - Livraisons
      summary: Colis Ã  livrer
      description: Liste des colis avec statut RAMASSE, prÃªts pour livraison
      parameters:
        - name: zone
          in: query
          schema:
            type: string
      responses:
        '200':
          description: Colis Ã  livrer
          content:
            application/json:
              schema:
                type: object
                properties:
                  zones:
                    type: array
                    items:
                      $ref: '#/components/schemas/LivraisonZone'
                  totalColis:
                    type: integer
                    example: 18
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'

  /admin/livraisons/{id}:
    get:
      tags:
        - Admin - Livraisons
      summary: DÃ©tails d'une livraison (admin)
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: DÃ©tails de la livraison
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LivraisonDetailResponse'
        '404':
          description: Livraison non trouvÃ©e
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'


  # ============================================
  # ADMIN - VENDEURS
  # ============================================
  /admin/vendeurs/en-attente:
    get:
      tags:
        - Admin - Vendeurs
      summary: Liste des vendeurs en attente de validation
      description: Vendeurs qui se sont inscrits mais n'ont pas encore Ã©tÃ© validÃ©s
      responses:
        '200':
          description: Liste des vendeurs en attente
          content:
            application/json:
              schema:
                type: object
                properties:
                  vendeurs:
                    type: array
                    items:
                      $ref: '#/components/schemas/VendeurDTO'
                  total:
                    type: integer
                    example: 5
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'

  /admin/vendeurs:
    get:
      tags:
        - Admin - Vendeurs
      summary: Liste de tous les vendeurs avec filtres
      description: Liste paginÃ©e de tous les vendeurs avec possibilitÃ© de filtrer
      parameters:
        - name: statut
          in: query
          schema:
            $ref: '#/components/schemas/StatutVendeur'
          description: Filtrer par statut
        - name: quartier
          in: query
          schema:
            type: string
          description: Filtrer par quartier
        - name: commune
          in: query
          schema:
            type: string
          description: Filtrer par commune
        - name: search
          in: query
          schema:
            type: string
          description: Recherche par nom, prÃ©nom, tÃ©lÃ©phone ou boutique
        - name: sort
          in: query
          schema:
            type: string
            enum: [dateInscription, nombreLivraisons, chiffreAffaires]
            default: dateInscription
          description: Tri
        - name: order
          in: query
          schema:
            type: string
            enum: [asc, desc]
            default: desc
        - name: page
          in: query
          schema:
            type: integer
            default: 0
        - name: size
          in: query
          schema:
            type: integer
            default: 20
      responses:
        '200':
          description: Liste paginÃ©e des vendeurs
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PageVendeur'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'

  /admin/vendeurs/{id}:
    get:
      tags:
        - Admin - Vendeurs
      summary: DÃ©tails complets d'un vendeur
      description: Informations dÃ©taillÃ©es + statistiques du vendeur
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: DÃ©tails du vendeur
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VendeurDetailDTO'
        '404':
          description: Vendeur non trouvÃ©
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'

  /admin/vendeurs/{id}/valider:
    post:
      tags:
        - Admin - Vendeurs
      summary: Valider un vendeur
      description: Change le statut du vendeur Ã  ACTIF, il peut dÃ©sormais utiliser la plateforme
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: Vendeur validÃ©
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                    example: "Vendeur validÃ© avec succÃ¨s"
                  vendeur:
                    $ref: '#/components/schemas/VendeurDTO'
        '400':
          description: Vendeur dÃ©jÃ  validÃ© ou statut incompatible
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Vendeur non trouvÃ©
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'

  /admin/vendeurs/{id}/suspendre:
    post:
      tags:
        - Admin - Vendeurs
      summary: Suspendre un vendeur
      description: Change le statut Ã  SUSPENDU, le vendeur ne peut plus utiliser la plateforme
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            format: int64
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - raison
              properties:
                raison:
                  type: string
                  example: "Non-paiement rÃ©pÃ©tÃ© des frais de livraison"
      responses:
        '200':
          description: Vendeur suspendu
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                    example: "Vendeur suspendu"
                  vendeur:
                    $ref: '#/components/schemas/VendeurDTO'
        '400':
          description: Statut incompatible
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Vendeur non trouvÃ©
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'

  /admin/vendeurs/{id}/bloquer:
    post:
      tags:
        - Admin - Vendeurs
      summary: Bloquer dÃ©finitivement un vendeur
      description: Change le statut Ã  BLOQUE, blocage permanent (fraude, etc.)
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            format: int64
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - raison
              properties:
                raison:
                  type: string
                  example: "Fraude confirmÃ©e - fausses livraisons"
      responses:
        '200':
          description: Vendeur bloquÃ©
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                    example: "Vendeur bloquÃ© dÃ©finitivement"
                  vendeur:
                    $ref: '#/components/schemas/VendeurDTO'
        '404':
          description: Vendeur non trouvÃ©
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'

  /admin/vendeurs/{id}/reactiver:
    post:
      tags:
        - Admin - Vendeurs
      summary: RÃ©activer un vendeur suspendu
      description: Remet le statut Ã  ACTIF pour un vendeur SUSPENDU
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: Vendeur rÃ©activÃ©
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                    example: "Vendeur rÃ©activÃ© avec succÃ¨s"
                  vendeur:
                    $ref: '#/components/schemas/VendeurDTO'
        '400':
          description: Vendeur non suspendu ou bloquÃ©
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Vendeur non trouvÃ©
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'

  # ============================================
  # ADMIN - FINANCES
  # ============================================
  /admin/finances/dashboard:
    get:
      tags:
        - Admin - Finances
      summary: Dashboard financier
      description: Vue d'ensemble des finances (cash collectÃ©, Ã  payer, commissions)
      parameters:
        - name: periode
          in: query
          schema:
            type: string
            enum: [jour, semaine, mois]
            default: jour
      responses:
        '200':
          description: Dashboard financier
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminFinancesDashboard'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'

  /admin/finances/paiements-pending:
    get:
      tags:
        - Admin - Finances
      summary: Demandes de paiement en attente
      description: Liste des vendeurs ayant demandÃ© un paiement
      responses:
        '200':
          description: Liste des demandes de paiement
          content:
            application/json:
              schema:
                type: object
                properties:
                  demandes:
                    type: array
                    items:
                      $ref: '#/components/schemas/DemandePaiement'
                  totalAPayer:
                    type: number
                    format: decimal
                    example: 8340000
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'

  /admin/finances/payer-vendeur/{vendeurId}:
    post:
      tags:
        - Admin - Finances
      summary: Payer un vendeur en cash
      description: Marque le paiement comme effectuÃ© aprÃ¨s remise du cash
      parameters:
        - name: vendeurId
          in: path
          required: true
          schema:
            type: integer
            format: int64
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - montant
              properties:
                montant:
                  type: number
                  format: decimal
                  example: 387500
                commentaire:
                  type: string
                  example: "Paiement en cash remis au vendeur"
      responses:
        '200':
          description: Paiement effectuÃ©
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                    example: "Paiement de 387,500 FCFA effectuÃ©"
                  reference:
                    type: string
                    example: "PAY-20251116-001"
        '400':
          description: Montant invalide ou solde insuffisant
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'

  /admin/finances/transactions:
    get:
      tags:
        - Admin - Finances
      summary: Historique des transactions
      parameters:
        - name: vendeurId
          in: query
          schema:
            type: integer
            format: int64
        - name: dateDebut
          in: query
          schema:
            type: string
            format: date
        - name: dateFin
          in: query
          schema:
            type: string
            format: date
        - name: page
          in: query
          schema:
            type: integer
            default: 0
        - name: size
          in: query
          schema:
            type: integer
            default: 50
      responses:
        '200':
          description: Liste des transactions
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PageTransaction'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'

  # ============================================
  # DELIVERY (Scan QR + Confirmation)
  # ============================================
  /delivery/{numeroTracking}:
    get:
      tags:
        - Delivery
      summary: RÃ©cupÃ©rer les infos de livraison (via QR scan)
      description: Endpoint appelÃ© aprÃ¨s scan du QR code, retourne les infos prÃ©-remplies
      security: []
      parameters:
        - name: numeroTracking
          in: path
          required: true
          schema:
            type: string
            example: "DKR-00567"
      responses:
        '200':
          description: Informations de livraison
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DeliveryInfoResponse'
        '404':
          description: Colis non trouvÃ©
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /delivery/{numeroTracking}/livrer:
    post:
      tags:
        - Delivery
      summary: Confirmer la livraison
      description: Marque le colis comme livrÃ© aprÃ¨s remise au client
      security: []
      parameters:
        - name: numeroTracking
          in: path
          required: true
          schema:
            type: string
            example: "DKR-00567"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ConfirmLivraisonRequest'
      responses:
        '200':
          description: Livraison confirmÃ©e
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                    example: "Livraison confirmÃ©e avec succÃ¨s"
                  numeroTracking:
                    type: string
                    example: "DKR-00567"
        '400':
          description: DonnÃ©es invalides ou colis pas dans le bon statut
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Colis non trouvÃ©
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  # ============================================
  # TRACKING PUBLIC
  # ============================================
  /tracking/{numeroTracking}:
    get:
      tags:
        - Tracking
      summary: Suivi public d'un colis
      description: Page de tracking accessible par le client final (pas d'auth requise)
      security: []
      parameters:
        - name: numeroTracking
          in: path
          required: true
          schema:
            type: string
            example: "DKR-00567"
      responses:
        '200':
          description: Informations de tracking
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TrackingResponse'
        '404':
          description: Colis non trouvÃ©
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  # ============================================
  # ZONES & QUARTIERS
  # ============================================
  /zones:
    get:
      tags:
        - Zones
      summary: Liste des zones de livraison
      security: []
      responses:
        '200':
          description: Liste des zones
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Zone'

  /quartiers/commune/{commune}:
    get:
      tags:
        - Zones
      summary: Liste des quartiers d'une commune
      description: Pour l'auto-complÃ©tion dans le formulaire
      security: []
      parameters:
        - name: commune
          in: path
          required: true
          schema:
            type: string
            example: "Dakar"
      responses:
        '200':
          description: Liste des quartiers
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Quartier'

  /tarifs/calculer:
    post:
      tags:
        - Zones
      summary: Calculer le tarif de livraison
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - zoneId
              properties:
                zoneId:
                  type: integer
                  format: int64
                urgence:
                  $ref: '#/components/schemas/TypeUrgence'
                poids:
                  type: number
                  format: double
                  example: 2.5
      responses:
        '200':
          description: Tarif calculÃ©
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TarifResponse'
        '400':
          description: DonnÃ©es invalides
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

    # ============================================
    # ADMIN - ZONES
    # ============================================
  /admin/zones:
    get:
      tags:
        - Admin - Zones
      summary: Liste de toutes les zones avec filtres
      description: Liste paginée des zones de livraison avec possibilité de filtrer
      parameters:
        - name: actif
          in: query
          description: Filtrer par zones actives/inactives
          schema:
            type: boolean
        - name: search
          in: query
          description: Recherche par nom ou description
          schema:
            type: string
        - name: page
          in: query
          schema:
            type: integer
            default: 0
        - name: size
          in: query
          schema:
            type: integer
            default: 20
      responses:
        '200':
          description: Liste paginée des zones
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PageZone'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
      security:
        - bearerAuth: [ ]

    post:
      tags:
        - Admin - Zones
      summary: Créer une nouvelle zone
      description: Création d'une nouvelle zone de livraison
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateZoneRequest'
      responses:
        '201':
          description: Zone créée avec succès
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminZonesPost201Response'
        '400':
          description: Données invalides
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
      security:
        - bearerAuth: [ ]

  /admin/zones/{id}:
    get:
      tags:
        - Admin - Zones
      summary: Détails d'une zone
      description: Informations détaillées d'une zone avec la liste de ses quartiers
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: Détails de la zone
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ZoneDetailDTO'
        '404':
          description: Zone non trouvée
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
      security:
        - bearerAuth: [ ]

    put:
      tags:
        - Admin - Zones
      summary: Modifier une zone
      description: Modification complète d'une zone (tous les champs)
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            format: int64
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateZoneRequest'
      responses:
        '200':
          description: Zone modifiée avec succès
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminZonesIdPut200Response'
        '400':
          description: Données invalides
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Zone non trouvée
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
      security:
        - bearerAuth: [ ]

    delete:
      tags:
        - Admin - Zones
      summary: Supprimer une zone
      description: Suppression d'une zone (impossible si elle a des quartiers ou livraisons associés)
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: Zone supprimée avec succès
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                    example: "Zone supprimée avec succès"
        '400':
          description: Impossible de supprimer (quartiers ou livraisons associés)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Zone non trouvée
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
      security:
        - bearerAuth: [ ]

  /admin/zones/{id}/tarifs:
    patch:
      tags:
        - Admin - Zones
      summary: Modifier les tarifs d'une zone
      description: Modification uniquement des tarifs (standard et express)
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            format: int64
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateTarifsRequest'
      responses:
        '200':
          description: Tarifs mis à jour avec succès
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminZonesIdTarifsPatch200Response'
        '400':
          description: Données invalides
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Zone non trouvée
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
      security:
        - bearerAuth: [ ]

  /admin/zones/{id}/toggle:
    patch:
      tags:
        - Admin - Zones
      summary: Activer/Désactiver une zone
      description: Change le statut actif/inactif d'une zone
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: Statut de la zone modifié
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminZonesIdTogglePatch200Response'
        '404':
          description: Zone non trouvée
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
      security:
        - bearerAuth: [ ]

  # ============================================
  # PROFIL UTILISATEUR (Vendeur + Admin)
  # ============================================
  /profil:
    get:
      tags:
        - Profil
      summary: Récupérer mon profil
      description: Retourne les informations du profil de l'utilisateur connecté (vendeur ou admin)
      responses:
        '200':
          description: Profil utilisateur
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProfilResponse'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
      security:
        - bearerAuth: [ ]

    put:
      tags:
        - Profil
      summary: Modifier mon profil
      description: Mise à jour des informations du profil utilisateur
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateProfilRequest'
      responses:
        '200':
          description: Profil mis à jour
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProfilPut200Response'
        '400':
          description: Données invalides
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
      security:
        - bearerAuth: [ ]

  /profil/password:
    patch:
      tags:
        - Profil
      summary: Changer mon mot de passe
      description: Modification du mot de passe de l'utilisateur connecté
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ChangePasswordRequest'
      responses:
        '200':
          description: Mot de passe modifié
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                    example: "Mot de passe modifié avec succès"
        '400':
          description: Ancien mot de passe incorrect
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
      security:
        - bearerAuth: [ ]
# ============================================
# COMPONENTS
# ============================================
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT

  responses:
    UnauthorizedError:
      description: Token manquant ou invalide
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'

    ForbiddenError:
      description: AccÃ¨s refusÃ© (permissions insuffisantes)
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'

  schemas:
    # ============================================
    # AUTH SCHEMAS
    # ============================================
    RegisterRequest:
      type: object
      required:
        - nom
        - prenom
        - telephone
        - password
        - commune
        - quartier
        - adresseComplete
      properties:
        nom:
          type: string
          example: "Diop"
        prenom:
          type: string
          example: "Fatou"
        telephone:
          type: string
          pattern: '^(77|78|76|70|75)[0-9]{7}$'
          example: "771234567"
        email:
          type: string
          format: email
          example: "fatou@example.com"
        password:
          type: string
          format: password
          minLength: 6
        nomBoutique:
          type: string
          example: "Fatou Fashion"
        categorieActivite:
          type: string
          example: "VÃªtements"
        instagram:
          type: string
          example: "@fatoufashion"
        facebook:
          type: string
          example: "Fatou Fashion Page"
        commune:
          type: string
          example: "Dakar"
        quartier:
          type: string
          example: "SacrÃ©-Coeur"
        adresseComplete:
          type: string
          example: "Rue SC-23, Villa 45"

    LoginRequest:
      type: object
      required:
        - telephone
        - password
      properties:
        telephone:
          type: string
          example: "771234567"
        password:
          type: string
          format: password

    AuthResponse:
      type: object
      properties:
        token:
          type: string
          example: "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
        refreshToken:
          type: string
        type:
          type: string
          example: "Bearer"
        user:
          $ref: '#/components/schemas/UserInfo'

    UserInfo:
      type: object
      properties:
        id:
          type: integer
          format: int64
        nom:
          type: string
        prenom:
          type: string
        telephone:
          type: string
        email:
          type: string
        role:
          $ref: '#/components/schemas/UserRole'
        nomBoutique:
          type: string
        statut:
          $ref: '#/components/schemas/StatutVendeur'

    # ============================================
    # LIVRAISON SCHEMAS
    # ============================================
    CreateLivraisonRequest:
      type: object
      required:
        - nomClient
        - telephoneClient
        - commune
        - quartier
        - adresseComplete
        - descriptionProduit
        - montantCOD
        - zoneId
      properties:
        nomClient:
          type: string
          example: "AÃ¯ssatou Ndiaye"
        telephoneClient:
          type: string
          pattern: '^(77|78|76|70|75)[0-9]{7}$'
          example: "772345678"
        commune:
          type: string
          example: "Dakar"
        quartier:
          type: string
          example: "Mermoz"
        adresseComplete:
          type: string
          example: "CitÃ© Biagui, Villa 45"
        pointRepere:
          type: string
          example: "Face Ã  la mosquÃ©e"
        descriptionProduit:
          type: string
          example: "Ensemble wax"
        fragile:
          type: boolean
          default: false
        poids:
          type: number
          format: double
          example: 1.5
        montantCOD:
          type: number
          format: decimal
          minimum: 1000
          maximum: 10000000
          example: 35000
        zoneId:
          type: integer
          format: int64
          example: 2
        urgence:
          $ref: '#/components/schemas/TypeUrgence'
        creneauSouhaite:
          type: string
          enum: [MATIN, APRES_MIDI, SOIR]
          example: "APRES_MIDI"
        notesPourLivreur:
          type: string
          example: "Appeler avant d'arriver"

    LivraisonResponse:
      type: object
      properties:
        id:
          type: integer
          format: int64
        numeroTracking:
          type: string
          example: "DKR-00567"
        qrCodeUrl:
          type: string
          example: "https://track.votreplateforme.sn/DKR-00567/deliver"
        statut:
          $ref: '#/components/schemas/StatutLivraison'
        dateCreation:
          type: string
          format: date-time
        fraisLivraison:
          type: number
          format: decimal
          example: 1500
        montantCOD:
          type: number
          format: decimal
          example: 35000
        montantARecevoir:
          type: number
          format: decimal
          description: Montant COD - Frais livraison
          example: 33500
        message:
          type: string
          example: "Livraison crÃ©Ã©e avec succÃ¨s ! Ã‰crivez #DKR-00567 sur votre colis."

    LivraisonDetailResponse:
      type: object
      properties:
        id:
          type: integer
          format: int64
        numeroTracking:
          type: string
        qrCodeUrl:
          type: string
        statut:
          $ref: '#/components/schemas/StatutLivraison'
        dateCreation:
          type: string
          format: date-time
        dateRamassage:
          type: string
          format: date-time
        dateLivraison:
          type: string
          format: date-time
        vendeur:
          type: object
          properties:
            id:
              type: integer
              format: int64
            nom:
              type: string
            prenom:
              type: string
            telephone:
              type: string
            nomBoutique:
              type: string
        client:
          type: object
          properties:
            nom:
              type: string
            telephone:
              type: string
            adresse:
              type: string
            pointRepere:
              type: string
        produit:
          type: object
          properties:
            description:
              type: string
            fragile:
              type: boolean
            poids:
              type: number
              format: double
        financier:
          type: object
          properties:
            montantCOD:
              type: number
              format: decimal
            fraisLivraison:
              type: number
              format: decimal
            cashCollecte:
              type: number
              format: decimal
        zone:
          type: string
        urgence:
          $ref: '#/components/schemas/TypeUrgence'
        commentaireLivraison:
          type: string

    PageLivraison:
      type: object
      properties:
        content:
          type: array
          items:
            $ref: '#/components/schemas/LivraisonResponse'
        page:
          type: integer
        size:
          type: integer
        totalElements:
          type: integer
        totalPages:
          type: integer

    # ============================================
    # VENDEUR SCHEMAS
    # ============================================
    VendeurDashboard:
      type: object
      properties:
        statistiquesJour:
          type: object
          properties:
            totalColis:
              type: integer
              example: 12
            colisLivres:
              type: integer
              example: 8
            colisEnCours:
              type: integer
              example: 4
        finances:
          type: object
          properties:
            soldeEnAttente:
              type: number
              format: decimal
              example: 145000
            prochainPaiement:
              type: string
              description: "Ã€ la demande du vendeur"
              example: "Sur demande"

    VendeurFinances:
      type: object
      properties:
        soldeEnAttente:
          type: number
          format: decimal
          example: 387500
        statistiquesMois:
          type: object
          properties:
            nombreLivraisons:
              type: integer
              example: 45
            caGenere:
              type: number
              format: decimal
              example: 1250000
            fraisLivraison:
              type: number
              format: decimal
              example: 67500
        historiquePaiements:
          type: array
          items:
            type: object
            properties:
              date:
                type: string
                format: date-time
              montant:
                type: number
                format: decimal
              reference:
                type: string
              statut:
                type: string
                enum: [EFFECTUE, EN_ATTENTE, ANNULE]

    # ============================================
    # ADMIN SCHEMAS
    # ============================================
    RamassageZone:
      type: object
      properties:
        zone:
          type: string
          example: "SacrÃ©-Coeur"
        nombreColis:
          type: integer
          example: 8
        vendeurs:
          type: array
          items:
            type: object
            properties:
              id:
                type: integer
                format: int64
              nom:
                type: string
              prenom:
                type: string
              telephone:
                type: string
              adresse:
                type: string
              nombreColis:
                type: integer
              colis:
                type: array
                items:
                  type: object
                  properties:
                    id:
                      type: integer
                      format: int64
                    numeroTracking:
                      type: string
                    nomClient:
                      type: string
                    adresseClient:
                      type: string
                    montantCOD:
                      type: number
                      format: decimal

    LivraisonZone:
      type: object
      properties:
        zone:
          type: string
          example: "Mermoz"
        nombreColis:
          type: integer
          example: 5
        colis:
          type: array
          items:
            type: object
            properties:
              id:
                type: integer
                format: int64
              numeroTracking:
                type: string
              nomClient:
                type: string
              telephoneClient:
                type: string
              adresse:
                type: string
              montantCOD:
                type: number
                format: decimal
              vendeur:
                type: string

    AdminFinancesDashboard:
      type: object
      properties:
        cashCollecte:
          type: number
          format: decimal
          example: 2340000
        aPayerVendeurs:
          type: number
          format: decimal
          example: 2180000
        commissions:
          type: number
          format: decimal
          example: 160000
        statistiques:
          type: object
          properties:
            nombreLivraisons:
              type: integer
            nombreVendeurs:
              type: integer
            tauxReussite:
              type: number
              format: double

    DemandePaiement:
      type: object
      properties:
        id:
          type: integer
          format: int64
        vendeur:
          type: object
          properties:
            id:
              type: integer
              format: int64
            nom:
              type: string
            prenom:
              type: string
            telephone:
              type: string
            nomBoutique:
              type: string
        montant:
          type: number
          format: decimal
        nombreLivraisons:
          type: integer
        dateDemande:
          type: string
          format: date-time

    PageTransaction:
      type: object
      properties:
        content:
          type: array
          items:
            type: object
            properties:
              id:
                type: integer
                format: int64
              vendeur:
                type: string
              montant:
                type: number
                format: decimal
              type:
                type: string
                enum: [PAIEMENT_VENDEUR, COLLECTE_COD, COMMISSION]
              reference:
                type: string
              statut:
                type: string
                enum: [EFFECTUE, EN_ATTENTE, ANNULE]
              date:
                type: string
                format: date-time
        page:
          type: integer
        size:
          type: integer
        totalElements:
          type: integer
        totalPages:
          type: integer


    # ============================================
    # VENDEUR SCHEMAS (Admin)
    # ============================================
    VendeurDTO:
      type: object
      properties:
        id:
          type: integer
          format: int64
          example: 15
        nom:
          type: string
          example: "Diop"
        prenom:
          type: string
          example: "Fatou"
        telephone:
          type: string
          example: "771234567"
        email:
          type: string
          example: "fatou@example.com"
        nomBoutique:
          type: string
          example: "Fatou Fashion"
        categorieActivite:
          type: string
          example: "VÃªtements"
        instagram:
          type: string
          example: "@fatoufashion"
        facebook:
          type: string
          example: "Fatou Fashion Store"
        commune:
          type: string
          example: "Dakar"
        quartier:
          type: string
          example: "Mermoz"
        adresseComplete:
          type: string
          example: "Mermoz, CitÃ© Biagui, Rue 45"
        statut:
          $ref: '#/components/schemas/StatutVendeur'
        dateInscription:
          type: string
          format: date-time
          example: "2025-12-01T10:30:00Z"
        valideLe:
          type: string
          format: date-time
          example: "2025-12-02T14:20:00Z"
        soldeEnAttente:
          type: number
          format: decimal
          example: 125000.00
        raisonSuspension:
          type: string
          example: "Non-paiement rÃ©pÃ©tÃ© des frais de livraison"

    VendeurDetailDTO:
      allOf:
        - $ref: '#/components/schemas/VendeurDTO'
        - type: object
          properties:
            validePar:
              type: object
              properties:
                id:
                  type: integer
                  format: int64
                nom:
                  type: string
                prenom:
                  type: string
            statistiques:
              type: object
              properties:
                nombreLivraisons:
                  type: integer
                  example: 47
                nombreLivraisonsReussies:
                  type: integer
                  example: 43
                nombreEnCours:
                  type: integer
                  example: 2
                tauxReussite:
                  type: number
                  format: double
                  example: 91.49
                  description: Pourcentage de livraisons rÃ©ussies
                chiffreAffairesTotal:
                  type: number
                  format: decimal
                  example: 2340000.00
                  description: Total des montants COD collectÃ©s
                derniereActivite:
                  type: string
                  format: date-time
                  example: "2025-12-06T16:45:00Z"
                  description: Date de la derniÃ¨re livraison crÃ©Ã©e

    PageVendeur:
      type: object
      properties:
        content:
          type: array
          items:
            $ref: '#/components/schemas/VendeurDTO'
        page:
          type: integer
          example: 0
        size:
          type: integer
          example: 20
        totalElements:
          type: integer
          example: 47
        totalPages:
          type: integer
          example: 3

    # ============================================
    # DELIVERY SCHEMAS
    # ============================================
    DeliveryInfoResponse:
      type: object
      properties:
        numeroTracking:
          type: string
          example: "DKR-00567"
        client:
          type: object
          properties:
            nom:
              type: string
              example: "AÃ¯ssatou Ndiaye"
            telephone:
              type: string
              example: "772345678"
            adresse:
              type: string
              example: "Mermoz, CitÃ© Biagui, Villa 45"
            pointRepere:
              type: string
              example: "Face Ã  la mosquÃ©e"
        produit:
          type: object
          properties:
            description:
              type: string
              example: "Ensemble wax"
            fragile:
              type: boolean
        vendeur:
          type: object
          properties:
            nom:
              type: string
              example: "Fatou"
        montantACollecter:
          type: number
          format: decimal
          example: 35000

    ConfirmLivraisonRequest:
      type: object
      required:
        - cashCollecte
        - colisRemis
      properties:
        cashCollecte:
          type: number
          format: decimal
          example: 35000
        colisRemis:
          type: boolean
          example: true
        commentaire:
          type: string
          example: "Livraison OK, cliente satisfaite"

    # ============================================
    # TRACKING SCHEMA
    # ============================================
    TrackingResponse:
      type: object
      properties:
        numeroTracking:
          type: string
          example: "DKR-00567"
        statut:
          $ref: '#/components/schemas/StatutLivraison'
        destination:
          type: string
          example: "Mermoz"
        montantAPayer:
          type: number
          format: decimal
          example: 35000
        timeline:
          type: array
          items:
            type: object
            properties:
              etape:
                type: string
                enum: [COMMANDE_CREEE, COLIS_RECUPERE, EN_COURS_LIVRAISON, LIVRE]
              date:
                type: string
                format: date-time
              effectue:
                type: boolean

    # ============================================
    # ZONE SCHEMAS
    # ============================================
    Zone:
      type: object
      properties:
        id:
          type: integer
          format: int64
        nom:
          type: string
          example: "Zone 2 - Sacré-Coeur, Mermoz"
        description:
          type: string
        communes: # ✅ AJOUTER CE CHAMP
          type: array
          items:
            type: string
          example: [ "Dakar", "Plateau", "Médina" ]
        tarifStandard:
          type: number
          format: decimal
          example: 1500
        tarifExpress:
          type: number
          format: decimal
          example: 2250

    Quartier:
      type: object
      properties:
        id:
          type: integer
          format: int64
        nom:
          type: string
          example: "Mermoz"
        commune:
          type: string
          example: "Dakar"
        zone:
          $ref: '#/components/schemas/Zone'

    TarifResponse:
      type: object
      properties:
        montant:
          type: number
          format: decimal
          example: 1500
        zone:
          type: string
        urgence:
          $ref: '#/components/schemas/TypeUrgence'
        detailCalcul:
          type: object
          properties:
            tarifBase:
              type: number
              format: decimal
            supplementPoids:
              type: number
              format: decimal
            supplementUrgence:
              type: number
              format: decimal

    # ============================================
    # ENUMS
    # ============================================
    UserRole:
      type: string
      enum:
        - VENDEUR
        - ADMIN

    StatutLivraison:
      type: string
      enum:
        - EN_ATTENTE_RAMASSAGE
        - RAMASSE
        - EN_ROUTE
        - LIVREE
        - ECHEC_ABSENT
        - ECHEC_REFUSE
        - ANNULEE

    TypeUrgence:
      type: string
      enum:
        - NORMAL
        - EXPRESS
      default: NORMAL

    StatutVendeur:
      type: string
      enum:
        - EN_ATTENTE_VALIDATION
        - ACTIF
        - SUSPENDU
        - BLOQUE
      description: |
        - EN_ATTENTE_VALIDATION: Vendeur inscrit mais pas encore valide par admin
        - ACTIF: Vendeur valide, peut utiliser la plateforme
        - SUSPENDU: Temporairement desactive (peut etre reaactive)
        - BLOQUE: Definitivement bloque (fraude, etc.)

    # ============================================
    # ERROR SCHEMA
    # ============================================
    ErrorResponse:
      type: object
      properties:
        timestamp:
          type: string
          format: date-time
        status:
          type: integer
        error:
          type: string
        message:
          type: string
        path:
          type: string

    # ============================================
    # SCHEMAS - Admin Zones
    # ============================================

    # DTOs pour Admin Zones
    ZoneAdminDTO:
      type: object
      properties:
        id:
          type: integer
          format: int64
          example: 1
        nom:
          type: string
          example: "Zone 1"
        description:
          type: string
          example: "Plateau, Ponty, Médina"
        tarifStandard:
          type: number
          format: decimal
          example: 1000.00
        tarifExpress:
          type: number
          format: decimal
          example: 1500.00
        actif:
          type: boolean
          example: true
        nombreQuartiers:
          type: integer
          example: 15
        dateCreation:
          type: string
          format: date-time
        dateModification:
          type: string
          format: date-time

    ZoneDetailDTO:
      type: object
      properties:
        id:
          type: integer
          format: int64
          example: 1
        nom:
          type: string
          example: "Zone 1"
        description:
          type: string
          example: "Plateau, Ponty, Médina"
        tarifStandard:
          type: number
          format: decimal
          example: 1000.00
        tarifExpress:
          type: number
          format: decimal
          example: 1500.00
        actif:
          type: boolean
          example: true
        quartiers:
          type: array
          items:
            type: object
            properties:
              id:
                type: integer
                format: int64
              nom:
                type: string
              commune:
                type: string
              actif:
                type: boolean
        dateCreation:
          type: string
          format: date-time
        dateModification:
          type: string
          format: date-time

    CreateZoneRequest:
      type: object
      required:
        - nom
        - tarifStandard
        - tarifExpress
        - actif
      properties:
        nom:
          type: string
          minLength: 3
          maxLength: 100
          example: "Zone 5"
        description:
          type: string
          maxLength: 500
          example: "Nouvelle zone - Keur Massar"
        tarifStandard:
          type: number
          format: decimal
          minimum: 0.01
          example: 2000.00
        tarifExpress:
          type: number
          format: decimal
          minimum: 0.01
          example: 3000.00
        actif:
          type: boolean
          example: true

    UpdateZoneRequest:
      type: object
      required:
        - nom
        - tarifStandard
        - tarifExpress
        - actif
      properties:
        nom:
          type: string
          minLength: 3
          maxLength: 100
          example: "Zone 1 - Centre ville"
        description:
          type: string
          maxLength: 500
          example: "Plateau, Ponty, Médina, HLM"
        tarifStandard:
          type: number
          format: decimal
          minimum: 0.01
          example: 1200.00
        tarifExpress:
          type: number
          format: decimal
          minimum: 0.01
          example: 1800.00
        actif:
          type: boolean
          example: true

    UpdateTarifsRequest:
      type: object
      required:
        - tarifStandard
        - tarifExpress
      properties:
        tarifStandard:
          type: number
          format: decimal
          minimum: 0.01
          example: 1500.00
        tarifExpress:
          type: number
          format: decimal
          minimum: 0.01
          example: 2200.00

    PageZone:
      type: object
      properties:
        content:
          type: array
          items:
            $ref: '#/components/schemas/ZoneAdminDTO'
        page:
          type: integer
          example: 0
        size:
          type: integer
          example: 20
        totalElements:
          type: integer
          example: 4
        totalPages:
          type: integer
          example: 1

    AdminZonesPost201Response:
      type: object
      properties:
        message:
          type: string
          example: "Zone créée avec succès"
        zone:
          $ref: '#/components/schemas/ZoneAdminDTO'

    AdminZonesIdPut200Response:
      type: object
      properties:
        message:
          type: string
          example: "Zone modifiée avec succès"
        zone:
          $ref: '#/components/schemas/ZoneAdminDTO'

    AdminZonesIdTarifsPatch200Response:
      type: object
      properties:
        message:
          type: string
          example: "Tarifs mis à jour avec succès"
        zone:
          type: object
          properties:
            id:
              type: integer
              format: int64
            nom:
              type: string
            tarifStandard:
              type: number
              format: decimal
            tarifExpress:
              type: number
              format: decimal

    AdminZonesIdTogglePatch200Response:
      type: object
      properties:
        message:
          type: string
          example: "Zone désactivée avec succès"
        zone:
          type: object
          properties:
            id:
              type: integer
              format: int64
            nom:
              type: string
            actif:
              type: boolean

    # ============================================
    # SCHEMAS - Profil
    # ============================================
    ProfilResponse:
      type: object
      properties:
        id:
          type: integer
          format: int64
        nom:
          type: string
          example: "Diop"
        prenom:
          type: string
          example: "Fatou"
        telephone:
          type: string
          example: "771234567"
        email:
          type: string
          example: "fatou@example.com"
        role:
          type: string
          enum: [ VENDEUR, ADMIN ]
        dateInscription:
          type: string
          format: date-time
        # Champs spécifiques vendeur (null si admin)
        nomBoutique:
          type: string
          example: "Fatou Fashion"
        categorieActivite:
          type: string
          example: "Vêtements"
        instagram:
          type: string
          example: "@fatoufashion"
        facebook:
          type: string
          example: "Fatou Fashion Dakar"
        commune:
          type: string
          example: "Dakar"
        quartier:
          type: string
          example: "Plateau"
        adresseComplete:
          type: string
          example: "Rue 10, Immeuble Seydi"
        statut:
          $ref: '#/components/schemas/StatutVendeur'
        soldeEnAttente:
          type: number
          format: decimal

    UpdateProfilRequest:
      type: object
      required:
        - nom
        - prenom
        - email
      properties:
        nom:
          type: string
          minLength: 2
          maxLength: 100
          example: "Diop"
        prenom:
          type: string
          minLength: 2
          maxLength: 100
          example: "Fatou"
        email:
          type: string
          format: email
          maxLength: 150
          example: "fatou@example.com"
        # Champs spécifiques vendeur (ignorés si admin)
        nomBoutique:
          type: string
          maxLength: 200
          example: "Fatou Fashion"
        categorieActivite:
          type: string
          maxLength: 100
          example: "Vêtements"
        instagram:
          type: string
          maxLength: 100
          example: "@fatoufashion"
        facebook:
          type: string
          maxLength: 200
          example: "Fatou Fashion Dakar"
        commune:
          type: string
          maxLength: 100
          example: "Dakar"
        quartier:
          type: string
          maxLength: 100
          example: "Plateau"
        adresseComplete:
          type: string
          maxLength: 500
          example: "Rue 10, Immeuble Seydi"

    ChangePasswordRequest:
      type: object
      required:
        - ancienPassword
        - nouveauPassword
      properties:
        ancienPassword:
          type: string
          format: password
          minLength: 6
          example: "ancien123"
        nouveauPassword:
          type: string
          format: password
          minLength: 6
          example: "nouveau456"

    ProfilPut200Response:
      type: object
      properties:
        message:
          type: string
          example: "Profil mis à jour avec succès"
        profil:
          $ref: '#/components/schemas/ProfilResponse'

