
# Base de données locale (PostgreSQL)
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/logistique_dakar
    username: logistique_user
    password: logistique2024
    driver-class-name: org.postgresql.Driver

  jpa:
    properties:
      hibernate:

  flyway:
    enabled: true

# Logging détaillé en DEV
logging:
  level:
    root: DEBUG
    sn.votreplateforme: DEBUG

# Configuration Application
app:
  base-url: http://localhost:8080
  tracking-url: http://localhost:4200/tracking

  # CORS (tout autorisé en développement)
  cors:
    allowed-origins: "*"
    allowed-methods: "*"
    allowed-headers: "*"
    allow-credentials: true
    max-age: 3600

# Configuration Twilio WhatsApp
twilio:
  enabled: true
  account-sid: AC4dc6a916fcac4bd65bb132b70477d922
  auth-token: 7237a04ea01a490138f2ce7250fb0d4f
  whatsapp:
    from: whatsapp:+14155238886
  admin:
    phone: +212676336628
